# Database Mode: 'connect' (use existing DB) or 'create' (auto-create DB and tables)
DB_MODE=connect

# Database Connection
DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=viewcounterdb
DB_USER=root
DB_PASSWORD=

# Server Configuration
PORT=3030
NODE_ENV=development

# Rate Limiting
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX=100

# Unique Visitor Tracking (hours)
# Set to 0 to disable duplicate prevention
UNIQUE_VISITOR_WINDOW_HOURS=24
